from "powershell" convert the terminal to "command prompt"

1. download node js and its .msi file available on its window
2. create a folder with frontend and backend.
3. inside the frontend folder, run the following:
        C:\Users\AASHI\OneDrive\Desktop\internship>cd frontend
        C:\Users\AASHI\OneDrive\Desktop\internship\frontend>npm create vite@latest .
        now select react then javascript.

4. a sever is generated http://localhost:5173/ where we can see our front end.

5. we also delete some useless files which came when we ran the command for vite.

6. the most important file is main.jsx   (jsx=html like language but easier than java script)


DAY 2

1. the name of file of component (.jsx file) begins with a capital letter.
2. unlike in html here it is not class but className

3. go to frontend folder in cmd terminal and then run "npm run dev" to see website.
4. UI always shows what is in app.jsx. if we make a different componet, the we write "export default Greetings"
   at the end of the new component and in app.jsx we import it 
5. html->main.jsx->app.jsx is  how things are connected
6. install react router
        C:\Users\AASHI\OneDrive\Desktop\internship\frontend>npm install react-router-dom
 also install: 
                npm install axios framer-motion react-hot-toast react-icons react-markdown recharts

and :           npm install -D @eslint/js eslint eslint-plugin-react-hooks eslint-plugin-react-refresh globals



7. git hub link   https://github.com/ankushk-cloud/SkillPath-Ai/tree/main










------------------------------backend----------------------------------

1. C:\Users\AASHI\OneDrive\Desktop\internship\backend>npm init
         (init means initialize)------my comment

         C:\Users\AASHI\OneDrive\Desktop\internship\backend>npm init
        This utility will walk you through creating a package.json file.
        It only covers the most common items, and tries to guess sensible defaults.

        See `npm help init` for definitive documentation on these fields and exactly what they do.

        Use `npm install <pkg>` afterwards to install a package and save it as a dependency in the package.json file.

        Press ^C at any time to quit.
        package name: (backend) 
        version: (1.0.0) 
        description: This is SkillPath AI backend
        entry point: (index.js) 
        test command: 
        git repository: 
        keywords: 
        author: 
        license: (ISC) 
        type: (commonjs) 
        About to write to C:\Users\AASHI\OneDrive\Desktop\internship\backend\package.json:

        {
        "name": "backend",
        "version": "1.0.0",
        "description": "This is SkillPath AI backend",
        "main": "index.js",
        "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
        },
        "author": "",
        "license": "ISC",
        "type": "commonjs"
        }


        Is this OK? (yes) 




2.  "npm install express" in the cmd terminal and we get new folder in our backend folder
3. write "node_module"  in gitignore file so that its not uploaded on github as its a heavy file.
4. node server.js ---->to run backend
5. if we make any changes in our server.js code then we need to close the server and restart it to see new changes.
        to solve this problem we use nodemon run: "npm install -g nodemon" and in package.json write "dev":"nodemon server.js" this helps us start the server with just "npm run dev".
        now in terminal type "npm run dev" to start server.

6. in server.js change ""type": "module"" to make our work easy.
7. run "npm i dotenv" in terminal. while within the backend folder
8. create a .env file and in .gitignore file write .env bcoz its sensitive file and we dont push them on github.

---------------------------------------------------------------------------------------

DATABASE: MONGODB

1. mongoDB is nosql(without tables it stores data ) database.
2. it stores data in key val pairs- space effective, it is paid
3. username: rajoraviplove78_db_user  password: 7gvuvLRgvio0TeZS   -------of mongodb
4. create free cluster-> copy code in .env file "mongo_uri= ...." .
5. install a packge of mongoose for mongodb :  "npm i mongoose"


6. create a folder models> User.js
7. we install a package bcrypt --> "npm i bcryptjs"

---------------------------------------------Routes in backend--------------------------

1. HTTP status codes
        200-ok success
        201-successfully created data
        400-bad request


2. main file=server.js -----(connected to)----router(has url path etc)----(conected to)----controller(has business logic)---(connected to)---models

3. controller handles api action.

4. java web token(jwt): header, payload, signature are components of JWT


